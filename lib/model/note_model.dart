class NoteModel {
  final String title, desc;
  final DateTime dateTime;
  final int color;

  NoteModel({
    required this.title,
    required this.desc,
    required this.dateTime,
    required this.color,
  });
}
